import 'package:flutter/material.dart';

class TranslationGameCategoryPage extends StatelessWidget {
  const TranslationGameCategoryPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
