import 'package:flutter/material.dart';

class TranslationGamePage extends StatelessWidget {
  const TranslationGamePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
